"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ping = ping;
const api_1 = __importDefault(require("./api"));
const error_1 = __importDefault(require("../error"));
/**
 * Will check if docker is listening on the socket path
 * @param throwErr whether to throw error. DEFAULT=false
 * @returns true if docker is listening
 */
function ping() {
    return __awaiter(this, arguments, void 0, function* (throwErr = false) {
        try {
            const _res = yield api_1.default.ping(); // _res is of type buffer
            const res = _res.toString();
            if (res === 'OK') {
                return true;
            }
            if (throwErr) {
                throw new error_1.default('Error pinging the server - 1', {
                    context: res
                });
            }
        }
        catch (er) {
            if (throwErr) {
                throw new error_1.default('Error pinging the server', {
                    original: er
                });
            }
        }
        return false;
    });
}
