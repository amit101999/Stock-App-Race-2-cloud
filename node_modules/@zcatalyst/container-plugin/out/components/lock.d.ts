import { EventEmitter } from 'events';
/**
 * Offers Simple lock function so that more than one process
 * may not be executed at the same time
 */
export declare class SimpleLock {
    #private;
    /** Event emitter to lock events */
    lockEvents: EventEmitter<[never]>;
    /**
     * Get a lock
     * @param id unique identifier of the resource to be locked
     * @returns true if a lock is acquired
     */
    getLock(id: string): boolean;
    /**
     * Wait for a locked component to be released
     * @param id unique identifier of the locked component
     * @returns Promise that resolves when the lock on the component is successfully released
     */
    waitRelease(id: string): Promise<void>;
    /**
     * Release the lock on a component
     * @param id unique identifier of the component to be released
     * @returns true
     */
    releaseLock(id: string): boolean;
}
