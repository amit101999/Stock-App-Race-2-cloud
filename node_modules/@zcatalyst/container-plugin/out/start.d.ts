import { RUOK } from './endpoints/container';
import { ContainerEvents } from './utils';
/**
 * start the catalyst function/appsail in a container
 * @param param0 details of the functions/appsail
 * @returns Promise that resolves to an event emitter
 */
export declare function start({ details, projectId, ruok }: {
    /** server details of the targets */
    details: Record<string, unknown>;
    /** current project Id */
    projectId: string;
    /** RUOK config */
    ruok?: RUOK;
}): Promise<ContainerEvents>;
