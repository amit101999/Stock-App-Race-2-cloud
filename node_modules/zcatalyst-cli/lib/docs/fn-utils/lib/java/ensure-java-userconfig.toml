[JAVA_CONFIG-1]
context = '''Unable to locate the binary path for stack ${arg[0]} in your system'''
aid = '''Please install the required JDK version and configure it using the config key ${arg[0]}.bin to serve or deploy the ${arg[0]} functions.'''

[JAVA_CONFIG-1A]
context = '''Unable to find the ${arg[3]} executable file in the bin path provided in the ${arg[0]} config
${arg[1]} - not found'''
aid = '''Please provide a valid java binary directory path for the config key ${arg[0]} to serve or deploy the ${arg[2]} functions.'''

[JAVA_CONFIG_2]
context='''The value for config ${arg[0]} cannot be the ${arg[1]} command'''
aid='''Please specify a valid java bin path for the stack ${arg[0]} config using the ${arg[2]} command\nExample: ${arg[3]}'''

[JAVA_CONFIG_JAVAC]
context = '''Unable to find the javac executable for the stack ${arg[0]} in your machine.'''
aid = '''Please provide a valid java binary path for the config key ${arg[0]} to serve or deploy the function.'''

[JAVA_CONFIG_MORE_RN]
context = '''Found more than one java runtime for java major version ${arg[0]} in the below paths:\n${arg[1]}'''
aid = '''Please configure the required java runtime bin folder path using the ${arg[2]} command\nExample: ${arg[3]}'''

[JAVA_CONFIG_V_MISS]
context = '''The version of java executable found in the bin path from the config ${arg[0]} does not match with the required stack'''
aid = '''Please provide a valid bin folder path for stack ${arg[1]} in the config ${arg[0]}'''
